<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= (settings && settings.seo_description) || 'Aula 2000 Építész Iroda — Családi házak, társasházak, ipari és középületek generál tervezése, beruházás lebonyolítása.' %>">
    <meta name="keywords" content="<%= (settings && settings.seo_keywords) || 'építész, építészet, generál tervezés, beruházás bonyolítás, 3D látványterv, műszaki ellenőrzés, energetikai tanúsítás, Szeged építész' %>">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= content.site_title %>">
    <meta property="og:description" content="<%= (settings && settings.seo_description) || 'Aula 2000 Építész Iroda — Generál tervezés és beruházás lebonyolítás 1995 óta.' %>">
    <meta property="og:image" content="<%= (settings && settings.hero_image_url) || '/icons/icon-512.png' %>">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="theme-color" content="<%= (settings && settings.pwa_theme_color) || '#d5ab5d' %>">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-title" content="<%= (settings && settings.pwa_short_name) || content.site_title || 'Aula 2000' %>">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">
    <link rel="preconnect" href="https://unpkg.com">
    <link rel="preconnect" href="https://images.unsplash.com">
    <link rel="dns-prefetch" href="https://fonts.googleapis.com">
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icons/icon-192.png" type="image/png">
    <link rel="apple-touch-icon" href="/icons/icon-192.png">
    <title><%= content.site_title %></title>
    <link rel="preload" href="/css/main.css?v=20260915c" as="style">
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,700;1,400&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="/vendor/fontawesome/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css?v=20260915c" media="all">
    <link rel="stylesheet" href="/css/contact-faq.css">
    <% if (settings.enable_dark_mode == '1') { %>
    <link rel="stylesheet" href="/css/theme-toggle.css">
    <script src="/js/theme-toggle.js" defer></script>
    <% } %>
    <style>
        :root {
            --gold: <%= (settings && settings.brand_primary_color) || '#d5ab5d' %>;
            --gold-strong: <%= (settings && settings.brand_primary_color) || '#d5ab5d' %>;
            --gold-soft: <%= (settings && settings.brand_secondary_color) || '#f9ebc8' %>;
            --text: <%= (settings && settings.brand_secondary_color) || '#f5efe8' %>;
        }
    </style>
    <link rel="stylesheet" href="/css/faq-fix.css">
